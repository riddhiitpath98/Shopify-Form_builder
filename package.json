{
  "name": "my-node-app",
  "version": "1.0.0",
  "main": "web/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "env-cmd -f .env shopify app build",
    "dev": "env-cmd -f .env shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app extension generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@shopify/app": "^3.0.0",
    "@shopify/cli": "^3.0.0",
    "cors": "^2.8.5",
    "env-cmd": "^10.1.0"
  }
}
